#!/usr/bin/env bash
set -e

mkdir -p chromium-lib

podman run \
  -it \
  --rm \
  --cap-add=SYS_ADMIN \
  -v ./chromium-lib:/var/lib/chromium \
  -e VNC_PASSWORD="vncpass" \
  -p 5900:5900 `# vnc port` \
  -p 9222:9222 `# cdp port` \
  xpod-chromium
